controller = {
    actions: {
        list: function() {
            // TODO: grab cities from store or models
            return AirView();
        },
        details: function(id) {
            // TODO: select details from store or models
            return AirView();
        }
    }
};